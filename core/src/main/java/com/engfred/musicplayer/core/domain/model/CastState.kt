package com.engfred.musicplayer.core.domain.model

/**
 * Represents the current connection state to a Google Cast device.
 */
enum class CastState {
    DISCONNECTED,
    CONNECTING,
    CONNECTED
}
